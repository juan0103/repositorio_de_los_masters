<%= form_with(model: pai, local: true) do |form| %>
  <% if pai.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pai.errors.count, "error") %> prohibited this pai from being saved:</h2>

      <ul>
      <% pai.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   
 <div class="panel panel-default">
        <div class="panel-heading">
            <h4>
                <strong>Crear Pais</strong></h4>
        </div>
        <div class="panel-body">
  
  <div class="form-horizontal">                      
  <div class="form-group">
     <label class="col-md-2 col-sm-4">
       <strong>Id:</strong>
     </label>
   <div class="col-md-6">
    <%= form.text_field :nombre, id: :pai_nombre %>
  </div>
</div>

  <div class="form-horizontal">                      
  <div class="form-group">
     <label class="col-md-2 col-sm-4">
       <strong>Nombre:</strong>
     </label>
   <div class="col-md-6">
    <%= form.number_field :id, id: :pai_id %>
  </div>
</div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
